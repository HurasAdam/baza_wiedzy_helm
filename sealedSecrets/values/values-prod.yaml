# Generic
namespace: server-prod

# Sealed secrets
sealed-secrets:
  namespace: "server-prod"
  resources:
    limits:
      cpu: 0.5
      memory: 1Gi
    requests:
      cpu: 0.25
      memory: 200M
