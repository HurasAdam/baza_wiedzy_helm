# Generic
namespace: server-dev

# Application dependencies
baza_wiedzyDeps:
  # Mongo
  db:
    deployment:
      name: bazaWiedzy-db-mongo
      replicas: 1
      image: mongo
      tag: 8.2-noble
      port: 3306
      limits:
        cpu: 1
        memory: 2Gi
      requests:
        cpu: 0.25
        memory: 200M
    pvc:
      name: bazaWiedzy-db-mongo-pvc
      storage: "10Gi"
    pv:
      name: bazaWiedzy-db-mongo-pv
      storage: "10Gi"
      hostPath: /home/jakub/pv/mongo
    auth:
      initRootPassword: bazaWiedzy-mongo-secret
      initRootUsername: bazaWiedzy-mongo-username
    service:
      name: bazaWiedzy-db-mongo-service
      port:
        name: bazaWiedzy-db-mongo-service-http
        value: 27017
