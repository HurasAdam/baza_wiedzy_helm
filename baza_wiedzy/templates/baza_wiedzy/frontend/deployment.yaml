apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.baza_wiedzyDeps.frontend.deployment.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Values.baza_wiedzyDeps.frontend.deployment.name }}
spec:
  progressDeadlineSeconds: 200
  replicas: {{ .Values.baza_wiedzyDeps.frontend.deployment.replicas }}
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: {{ .Values.baza_wiedzyDeps.frontend.deployment.name }}
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: {{ .Values.baza_wiedzyDeps.frontend.deployment.name }}
    spec:
      containers:
        - name: {{ .Values.baza_wiedzyDeps.frontend.deployment.name }}
          image: "{{ .Values.baza_wiedzyDeps.frontend.deployment.image }}:{{ .Values.baza_wiedzyDeps.frontend.deployment.tag }}"
          imagePullPolicy: Always
          ports:
            - containerPort: {{ .Values.baza_wiedzyDeps.frontend.deployment.port }}
          resources:
            limits:
              cpu: "{{ .Values.baza_wiedzyDeps.frontend.deployment.limits.cpu }}"
              memory: "{{ .Values.baza_wiedzyDeps.frontend.deployment.limits.memory }}"
            requests:
              cpu: "{{ .Values.baza_wiedzyDeps.frontend.deployment.requests.cpu }}"
              memory: "{{ .Values.baza_wiedzyDeps.frontend.deployment.requests.memory }}"
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      terminationGracePeriodSeconds: 60
