apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.forgejoDeps.db.service.name }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.forgejoDeps.db.deployment.name }}
  ports:
    - name: {{ .Values.forgejoDeps.db.service.port.name }}
      protocol: TCP
      port: {{ .Values.forgejoDeps.db.deployment.port }}
      targetPort: {{ .Values.forgejoDeps.db.service.port.value }}
