apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.baza_wiedzyDeps.backend.service.name }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.baza_wiedzyDeps.backend.deployment.name }}
  ports:
    - name: {{ .Values.baza_wiedzyDeps.backend.service.port.name }}
      protocol: TCP
      port: {{ .Values.baza_wiedzyDeps.backend.deployment.port }}
      targetPort: {{ .Values.baza_wiedzyDeps.backend.service.port.value }}
