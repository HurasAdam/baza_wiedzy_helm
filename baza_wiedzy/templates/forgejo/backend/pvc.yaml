apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.forgejoDeps.navidrome.data.pvc.name }}
  namespace: {{ .Values.namespace }}
spec:
  storageClassName: ""
  volumeName: {{ .Values.forgejoDeps.navidrome.data.pv.name }}
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.forgejoDeps.navidrome.data.pvc.storage }}
